#A função quando está num arquivo separado deve ter o mesmo nome do arquivo
#Deve-se iniciar com a palavra function
function [x, y] = FuncaoRetorno(num1 , num2)
  x = num1 + num2;
  y = num1 - num2;
endfunction
